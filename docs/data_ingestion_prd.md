Data ingestion

Improve data ingestion pipeline by somehow being able to take the uploaded .docx, .pdf, .csv, .xslx, .txt files and somehow extract and organize critical data and specific numbers into a centralized table structure (like file_id, type_doc, invoice_amount etc.) that should speed up the ingestion fo data for COGNEE and the LLM (for example the LLM can read human text and critical data separtely) as well as saving these tables somwhere to present then in the output so if the user desires to see a specific file in its fullest but organized in a table (it should be able to switch between original file and table version). This should also help with the data preprocessing that will be necessary for training a ML model if its decided to be implemented and also improve data ingestion from quaterly reports analysis